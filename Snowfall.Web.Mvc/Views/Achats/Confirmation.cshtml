@model Snowfall.Web.Mvc.Models.Achats.ConfirmationViewModel

@{
    ViewBag.Title = "Confirmation de commande";
    Layout = "_Layout";
}

<div class="text-center mb-5">
    <div class="display-1 text-success mb-3">✓</div>
    <h2>Merci pour votre achat!</h2>
    <p class="text-muted">Commande #@Model.Achat.Id — @Model.Achat.CreatedAt.ToString("D")</p>
</div>

<div class="row g-4 justify-content-center">
    <!-- Informations de livraison -->
    @if (Model.Achat.Utilisateur != null)
    {
        <div class="col-md-5">
            <div class="card h-100">
                <div class="card-header">
                    <h5 class="mb-0">Livraison</h5>
                </div>
                <div class="card-body">
                    <p><strong>@Model.Achat.Utilisateur.Prenom @Model.Achat.Utilisateur.Nom</strong></p>
                    <p class="mb-1">@Model.Achat.Utilisateur.Email</p>
                    <p class="mb-1">@Model.InformationClient.Adresse</p>
                    <p class="mb-1">@Model.InformationClient.Ville, @Model.InformationClient.Province &nbsp; @Model.InformationClient.CodePostal</p>
                    <p class="mb-0">@Model.InformationClient.Pays</p>
                </div>
            </div>
        </div>
    }

    <!-- Détail de la facture -->
    <div class="col-md-7">
        <div class="card">
            <div class="card-header">
                <h5 class="mb-0">Détail de la facture</h5>
            </div>
            <div class="card-body p-0">
                <table class="table mb-0">
                    <thead class="table-light">
                        <tr>
                            <th>Événement</th>
                            <th class="text-center">Qté</th>
                            <th class="text-end">Prix unit.</th>
                            <th class="text-end">Sous-total</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var ligne in Model.Achat.LignesAchat ?? [])
                        {
                            <tr>
                                <td>@ligne.Evenement?.Nom</td>
                                <td class="text-center">@ligne.Quantite</td>
                                <td class="text-end">@ligne.PrixUnitaire.ToString("C")</td>
                                <td class="text-end">@((ligne.PrixUnitaire * ligne.Quantite).ToString("C"))</td>
                            </tr>
                        }
                    </tbody>
                    <tfoot class="table-light">
                        <tr>
                            <td colspan="3" class="text-end"><strong>Sous-total</strong></td>
                            <td class="text-end">@Model.Achat.SousTotal.ToString("C")</td>
                        </tr>
                        <tr>
                            <td colspan="3" class="text-end"><strong>Livraison</strong></td>
                            <td class="text-end">@(Model.Achat.Livraison == 0 ? "Gratuite" : Model.Achat.Livraison.ToString("C"))</td>
                        </tr>
                        <tr class="table-success">
                            <td colspan="3" class="text-end"><strong>Total payé</strong></td>
                            <td class="text-end"><strong>@Model.Achat.Total.ToString("C")</strong></td>
                        </tr>
                    </tfoot>
                </table>
            </div>
        </div>
    </div>
</div>

<div class="text-center mt-4">
    <a asp-controller="Evenements" asp-action="Index" class="btn btn-primary">Retour aux événements</a>
</div>

